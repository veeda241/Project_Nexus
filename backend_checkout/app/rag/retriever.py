"""
Retriever
=========
Queries the ChromaDB vector store with an embedded user question.
Returns the top-k most relevant chunks across all modalities,
with optional metadata filtering (by document, modality, date, etc.).
"""
