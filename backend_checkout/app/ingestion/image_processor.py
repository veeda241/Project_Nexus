"""
Image Processor
===============
Processes uploaded images (PNG, JPEG, WEBP). Uses CLIP (via
transformers) to generate image embeddings and optionally
extracts OCR text using Tesseract or a vision model.
"""
